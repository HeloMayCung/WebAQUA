@model WebThuySinh.Models.Catagories


@{


}

<style>
    .KichthuocForm {
        width: 600px;
        margin: auto;
        margin-top: 100px;
        grid-auto-columns: 20px;
    }
</style>


@using (Html.BeginForm("Update", "Catagory", new {  enctype = "multipart/dorm-data", @class = "KichthuocForm" }, FormMethod.Post))
{
        <!-- Nội dung của form -->


    <div class="d-flex align-items-center justify-content-center"><h5>Form Update</h5></div>
    <form style="width:500px">
        @*<div class="form-group row  justify-content-around column-gap-sm-3">
            <label for="inputEmail3" class="col-sm-2 col-form-label">CategoryID</label>
            <div class="col-sm-10">
                <div contenteditable="false">
                    @Html.TextBoxFor(row => row.CategoryID, new { @class = "form-control", placeholder = "CategoryID" })
                </div>
            </div>
        </div>*@
        <div class="form-group row  justify-content-around column-gap-sm-3">
            <label for="inputPassword3" class="col-sm-2 col-form-label">CategoryName</label>
            <div class="col-sm-10">
                @Html.TextBoxFor(row => row.CategoryName, new { @class = "form-control", placeholder = "CategoryName" })
            </div>
        </div>


        <div class="form-group row  justify-content-around ">
            <div class="col-sm-10">
                <button type="submit" class="btn btn-primary">Update</button>
            </div>
        </div>
    </form>


}
